# ─────────────────────────────────────────────────────────────────────────────
# Digital Guardian — Backend Dependencies
# FastAPI + Async SQLite + Pydantic v2
# ─────────────────────────────────────────────────────────────────────────────

# Web Framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Pydantic v2 (built-in to FastAPI 0.111+, explicit for clarity)
pydantic==2.7.1
pydantic-settings==2.3.0

# Database (Async SQLite)
sqlalchemy==2.0.30
aiosqlite==0.20.0

# HTTP Client (for Phase 2 LLM calls to Ollama)
httpx==0.27.0

# Environment
python-dotenv==1.0.1
